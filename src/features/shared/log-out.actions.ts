import { createElement } from '@core/proxy/ProxyFactory';

const folder = 'shared';
const file = 'log-out';

export const LogOutActions = {
  logOutButton: createElement(folder, file, 'logOutButton'),

};